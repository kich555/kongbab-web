import { createStyles } from '@mantine/core';

const useConsultationFormStyles = createStyles((theme) => ({
  font20: {
    fontSize: '20px',
  },
  lineHeight150: {
    lineHeight: '150%',
  },
}));

export default useConsultationFormStyles;

// border: 1px solid #EEEEEE;
