import { createStyles } from '@mantine/core';

const useIntroduceSectionStyles = createStyles((theme) => ({
  font18: {
    fontSize: '18px',
  },
  lineHeight150: {
    lineHeight: '150%',
  },
}));

export default useIntroduceSectionStyles;
