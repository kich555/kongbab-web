import { createStyles } from '@mantine/core';

const useDefaultButtonStyles = createStyles((theme) => ({
  defaultBtn: {
    width: '140px',
    height: '44px',
    textAlign: 'center',
  },
}));

export default useDefaultButtonStyles;
